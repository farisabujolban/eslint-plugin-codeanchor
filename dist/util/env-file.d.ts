export declare function loadEnvKeys(fileBeingLinted: string, envFiles?: string[]): Set<string>;
/** Clears the cache — used in tests to reset state between runs. */
export declare function clearEnvCache(): void;
